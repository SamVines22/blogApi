<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
</head>
<body>
    <%-include('template')%> 
    <%if (name) {%>
        <h2>Welcome back <%=name%></h2>
        <a href = "/user/logout">Logout</a>
    <%}else {%>
        <h2>Welcome User!</h2>
        <a href = "/user">Click here to login</a>
        <a href = "/user/signup">Click here to sign up</a>
        <%}%>
    <ul>
    <% posts.map((item)=> { %>

        <li><a href = "/posts/<%=item.id%>"><%=item.title%></a></li>

    <%})%>
    </ul>
</body>
</html>